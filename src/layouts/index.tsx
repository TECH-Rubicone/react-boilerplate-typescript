// outsource libraries
import React, { memo } from 'react';

// local dependencies
import App from './app';

const Layouts = () => {
  return <div>
    <h1>Layouts</h1>
    <App />
  </div>;
};

export default memo(Layouts);
